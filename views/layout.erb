<!doctype html>
<html>
  <head>
    <title><%= content_for(:title) || "Stocks" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  </head>
  <body>
    <h1><%= content_for(:title) || "Stocks" %></h1>
    <%== yield %>
    <%= content_for(:footer) || render("footer") %>
  </body>
</html>
